To transition this robust framework from structural stability (driven by mechanical refuges and forced respawns) to true emergent intelligence (where behaviors drive survival and stable macro-dynamics), the simulation must shift away from artificial safety nets.If the current mechanical refuge while-loops are turned off, the ecosystem collapses instantly because the policies are "PPO-lite" networks behaving like random walks. Moving to emergence requires production-grade algorithms, realistic biological constraints, and asymmetric behaviors.The Core Strategic Shifts1. Removing the Safety Wheels (The Anti-Collapse Shift)In eco_engine.cpp, the artificial while loops that forcefully replenish populations up to a baseline floor must be completely removed. Stability must instead come from environmental topology and sensory limits. If a species goes extinct, the episode terminates, issuing a harsh terminal penalty to the entire dead population's history buffer.2. Upgrading to Full PPO + Generalized Advantage Estimation (GAE)The "PPO-lite" implementation must be replaced with full Clipped PPO. Crucially, GAE ($\lambda$) must be implemented to smooth out the credit-assignment problem across long-horizon survival tasks, allowing a fox to learn that tracking a rabbit over 20 steps is valuable even if it hasn't eaten yet.3. Asymmetric Evolution (Per-Species Mechanics)Foxes and rabbits must have distinct physical trade-offs in eco_engine.cpp:Rabbits: Faster turn rates or a slight speed advantage, but must constantly move to gather static grass.Foxes: Slower or heavier turns, higher acceleration/sprint energy costs, but a wider vision cone.Emergent MARL Optimization RoadmapTask 1: Complete Production PPO Upgrade with GAEDescription: Replace the single-step return ($r + \gamma \cdot V$) in eco_marl.py::update with a full episodic buffer system. Implement Generalized Advantage Estimation ($\mathcal{A}_t^{\text{GAE}}$), PPO clip objective matching the standard formulation ($1-\epsilon, 1+\epsilon$), policy entropy bonuses to drive exploration, and value function clipping.Acceptance Criteria: * The policy successfully optimizes a standard gym verification environment or a single-agent benchmark with identical hyper-parameters.Explained variance ($\frac{\text{Var}(\text{Returns} - \text{Values})}{\text{Var}(\text{Returns})}$) scales cleanly past $0.8$ on long-horizon collection runs.Base Metric & Value: Policy Training Loss Stability $\rightarrow$ Baseline: High gradient variance / unclipped loss spikes in PPO-lite.Expected Post-Task Value: Smoothly decaying policy loss; Entropy values showing controlled decay over iterations.Most Critical Metric to Safeguard: Inference and Optimization Time per Iteration.Guardrail against degradation: The GAE buffer calculation must be vectorized using NumPy/PyTorch operations. Optimization overhead must not increase total iteration time by more than 15% over the current baseline.Task 2: C++ Asymmetric Physics & Sensory Horizon TuningDescription: Modify eco_engine.cpp to separate the physical properties of foxes and rabbits. Split the uniform repro_threshold into two explicit variables. Reduce the sensory vision radius of both species to introduce spatial hide-and-seek mechanics. Implement a continuous acceleration decay (inertia) so foxes cannot instantly reverse direction, allowing agile rabbits to out-maneuver them.Acceptance Criteria:Shuffling entity indices continues to show exact permutation invariance ($0.00e+00$) in test_policy.py.C++ profile verifies asymmetric velocity caps are respected during the step execution loop.Base Metric & Value: Base Random-Walk Collapse Horizon $\rightarrow$ Baseline: System collapses to extinction in $<500$ steps without mechanical safety loops.Expected Post-Task Value: Extinction horizon extends to $\ge 2,500$ steps purely due to physical asymmetry and sensory limits (rabbits can successfully run out of vision range).Most Critical Metric to Safeguard: Bit-Identical Spatial Hash Grid Matching.Guardrail against degradation: Run test_eco.py::test_grid_correctness. The updated asymmetric physics step loop must remain 100% bit-identical to the brute-force $O(N^2)$ verification pass. Physical property divergence must not introduce float rounding errors across spatial partitions.Task 3: Value-Function Bootstrapping for Birth and DeathDescription: Fix the credit assignment problem during reproduction and death events within eco_marl.py. When a parent entity undergoes binary fission, it splits its trajectory memory; the child agent must bootstrap its initial critic value from the parent’s current state embedding. When an entity dies, its final step must log a harsh negative terminal reward ($-10.0$), and its trajectory must be forcefully closed and padded for the PPO optimization pass.Acceptance Criteria:test_marl.py verifies no cross-contamination of gradient updates when processing split agent trajectories.Parent-to-child value function weight hand-offs match with $0$ memory pointer corruption.Base Metric & Value: Average Lifetime per Entity $\rightarrow$ Baseline: $\sim 80$ steps per entity under PPO-lite.Expected Post-Task Value: Average entity lifetime scales past $\ge 400$ steps as agents learn long-horizon self-preservation behavior.Most Critical Metric to Safeguard: Trajectory Buffer Integrity.Guardrail against degradation: Assert that the sudden death of 50 agents in a single tick does not create ragged array shapes or misaligned tensor slices in Python. Any mismatch between tracking IDs and tensor rows must throw a hard runtime error rather than silent truncation.Task 4: Mechanical Refuge Deactivation & Emergent Oscillation ValidationDescription: Completely comment out the refuge_rabbits and refuge_foxes while loops at the end of eco_engine.cpp::step(). Train the updated architecture on train_eco.py for a sustained $1,000+$ iterations on GPU. The agents must now learn to balance their behavior dynamically: foxes must learn to "forage" softly when rabbit populations are low rather than over-eating them into extinction, and rabbits must optimize predator avoidance.Acceptance Criteria:System demonstrates multi-wave Lotka-Volterra population oscillations across continuous horizons without a single mechanical respawn intervention.Base Metric & Value: System Collapse Rate without Refuges $\rightarrow$ Baseline: 100% collapse to 0 population within 1,000 frames.Expected Post-Task Value: 0% collapse rate across a continuous 50,000-frame evaluation run.Most Critical Metric to Safeguard: Macro Population Co-Correlation.Guardrail against degradation: Calculate the cross-correlation between the Fox and Rabbit population curves. The cross-correlation peak must reflect a clear phase lag (Rabbits peak $\rightarrow$ Foxes peak later), with a wave correlation coefficient of $\ge 0.65$. If the correlation drops or turns negative, it signifies that the populations are moving randomly rather than maintaining an intelligent, feedback-driven biological ecosystem.
